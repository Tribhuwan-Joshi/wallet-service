{
  "name": "wallet-service",
  "version": "1.0.0",
  "description": "Internal wallet service for high-traffic application",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "migrate": "dotenv -e .env.development -- node-pg-migrate up",
    "migrate:down": "dotenv -e -env.development -- node-pg-migrate down",
    "dev": "NODE_ENV=development nodemon src/server.js",
    "prod": "NODE_ENV=production nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "pg": "^8.18.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "node-pg-migrate": "^8.0.4"
  }
}
